{
  "name": "calendarbackend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "../CalendarFrontEnd/client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js"
  },
  "author": "LS",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "morgan": "^1.9.0",
    "pg-promise": "^8.4.4"
  },
  "engines": {
    "node": "9.4.0"
  }
}
